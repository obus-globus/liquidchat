name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: lint + type-check + tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up uv
        uses: astral-sh/setup-uv@caf0cab7a618c569241d31dcd442f54681755d39 # v3
        with:
          enable-cache: true

      - name: Sync deps
        run: uv sync --frozen --dev --all-extras

      - name: ruff (lint)
        run: uv run ruff check .

      - name: ruff (format)
        run: uv run ruff format --check .

      - name: basedpyright
        run: uv run basedpyright

      - name: pytest (unit tests only — integration tests need axochat)
        run: uv run pytest -q -m "not integration" --cov --cov-report=term-missing --cov-report=xml

      - name: deptry (dependency hygiene)
        run: uv run deptry .

      - name: pip-audit (supply-chain CVE scan)
        run: uv run pip-audit --skip-editable

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: coverage
          path: coverage.xml

  build:
    name: build wheel
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: astral-sh/setup-uv@caf0cab7a618c569241d31dcd442f54681755d39 # v3
      - run: uv build
      - name: Verify py.typed in wheel
        run: |
          uv run python -c "import zipfile,glob; [print(zipfile.ZipFile(w).namelist()) for w in glob.glob('dist/*.whl')]" | grep py.typed
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: dist
          path: dist/
